# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editors
.idea/
.vscode/
*.swp
*.swo
*~

# Backups written by the installers when they edit a shell config
*.bak
*.bak-*

# Local test scratch
.installer.log

# Local tool state. These can contain absolute home paths, so keep the rule in
# the repository rather than relying on a contributor's global gitignore.
.claude/settings.local.json
.venv/
venv/
